# Convex deployment URL (no secrets — fill in locally, never commit)
CONVEX_URL=

# Optional: named deployment from `npx convex` (e.g. after `convex dev` login)
# CONVEX_DEPLOYMENT=

# Optional agent / provider keys (local only)
# AGENT_API_KEY=
# FIRECRAWL_API_KEY=
